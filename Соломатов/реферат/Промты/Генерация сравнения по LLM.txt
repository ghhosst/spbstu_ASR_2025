ROLE: Эксперт в области системного программирования, компиляторов и интеграции LLM. Ты специализируешься на написании научных статей и технической документации.

CONTEXT: Я работаю над проектом DslTools. Это среда для разработки DSL, где семантические обработчики (Visitor) генерируются автоматически с помощью LLM. Мне нужно написать раздел для научной работы, описывающий выбор моделей и процесс генерации промтов.

INPUT DATA: Тебе необходимо проанализировать следующие предоставленные файлы:

Report.pdf — общее описание системы DslTools и примеры грамматик.

Документ "Открытые LLM..." — сравнительный анализ моделей.

rbnfgo.py и ast.py — примеры реализации механизма Visitor и AST в моей системе.

example.yaml — пример CST-дерева, которое служит входными данными для генерации.

TASK: Напиши раздел научной работы на русском языке, строго придерживаясь формата LaTeX.

STRUCTURE & CONSTRAINTS:

Subsection: "Модуль автоматической генерации семантических обработчиков"

Проведи краткий аналитический обзор на основе документа "Открытые LLM...". Сократи исходный текст, оставив только ключевые аргументы.

Включи таблицу (LaTeX table + tabular) со сравнением характеристик моделей из документа.

Сделай итоговый вывод абзацем: для проведения тестов была выбрана модель DeepSeek API в качестве мощного внешнего решения и Qwen в качестве эффективной локальной модели.

Важно: Удали любые ссылки вида [cite_...] и внешние гиперссылки.

Subsubsection: "Разработка шаблонов промтов для генерации кода"

На основе анализа rbnfgo.py, ast.py и example.yaml сформулируй примеры системных промтов (prompts), которые DslTools отправляет в LLM.

Промты должны инструктировать модель генерировать методы visit_... для трансляции узлов CST в код LaTeX.

Оформи примеры промтов в окружении verbatim или lstlisting.

TECHNICAL REQUIREMENTS:

Выходной формат: чистый код LaTeX.

Стиль текста: научный, формальный.

Избегай использования ссылок на файлы как на документы (вместо "в файле Report.pdf" пиши "в рамках архитектуры системы").

Весь текст должен быть на русском языке.