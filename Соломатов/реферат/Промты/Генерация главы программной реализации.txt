Role: Lead Software Architect & Full-stack Developer.

Context: Подготовка главы «Программная реализация» для НИР по проекту DslTools. Система представляет собой Unified Executable Parsing Environment (UEPE) для динамической разработки и исполнения DSL.

Task: Напиши технический текст для следующих подразделов главы, используя предоставленные данные и архитектурные принципы. Стиль: технический отчет/НИР, использование LaTeX.

Input Data (для анализа):
1. Use-Cases (из Lab3): Акторы (BaseUser, TargetUser, Creator, StrongCreator) и их взаимодействия.
2. UI Design (из Lab5): Описание интерфейса (на базе Microsoft ScriptBook), включая зоны редактора грамматики, редактора кода и визуализации.
3. Architecture Notes: Модульная архитектура, использование интерфейсов для каждого этапа конвейера (Pipeline), механизм Hot Reload, контейнеризация (Docker).

Sections to Write:

\subsection{Основные сценарии использования}
- На основе данных Lab3 опиши жизненный цикл работы в системе:
  - Сценарий разработчика (Creator): описание грамматики -> автоматическая сборка -> верификация через визуализацию CST/AST.
  - Сценарий пользователя (TargetUser): запуск готовой программы на DSL.
  - Сценарий эксперта (Strong Creator): модификация алгоритмов ядра через подмену модулей.
- Сфокусируйся на бесшовности перехода между редактированием и исполнением.

\subsection{Архитектура приложения}
- Опиши модульную структуру системы: каждый этап конвейера(отдельный модуль Core / Backend) (Tokenizer, CSTCreator, AST Translater) скрыт за абстрактным интерфейсом. Обоснуй это необходимостью легкой замены алгоритмов (например, замена стандартного лексера на кастомный). Кроме того нужно написать про GrammarObject - сущность описывающая грамматику (проанализируй файл parse.py) и предложи описание на его основе.
+ Раскрой механизм Hot Reload: как система отслеживает изменения в модулях и инициирует перезапуск. 
+ Опиши стратегию инстанцирования: при перезагрузке (Reload) система запускает новый изолированный инстанс интерпретатора для обеспечения чистоты состояния.
- Укажи на использование контейнеризации: UI-часть вынесена в отдельный контейнер (модуль) для изоляции интерфейса от вычислительного ядра. Back-end инстансы тоже в отдельных контейнерах поднимаются.
- Упомяни запуск всей среды на Localhost.

\subsection{Пользовательский интерфейс}
- Опиши логику построения UI в стиле "Language Workbench". 
- Выдели основные функциональные зоны:
  - Панель мета-моделирования (редактор EBNF).
  - Панель прикладного программирования (редактор целевого кода).
  - Интерактивное окно визуализации CST/AST и графов правил вывода (не представлено в UI).
  - Панель диагностических сообщений и логов.

\subsection{Пример задания грамматики целевого языка}

- Приведи конкретный пример грамматики на EBNF для простого языка (из примера pseco.rbnf) - оформь в виде листинга кода.
+ допиши текст для примера сгенерированного дерева разбора (правило Grammar) - достаточно просто заготовки текста, которая будет логично вытекать из примера грамматики.
- Напиши текст для описания примера использования паттерна Visiter для этого правила. Нужен опятьже только текст который бы логично вытекал из прошлого пункта и который бы идеально смотрелся с дальнейшим листингом Visitor.

\subsection{Примеры программ на целевом языке}
Укажи пример из файла (test.txt) В качестве листинга
Плюс к тому после этого примера должна идти картинга дерева разбора этой программы напиши тоже текст.

Formatting Constraints:
- Все термины (Hot Reload, Pipeline, UEPE, Docker) вводи при первом упоминании.
- Используй LaTeX для оформления схем (по возможности текстом) и листингов кода.
- Акцентируй внимание на Hot Reload как на ключевой фиче для быстрой итерации разработки.